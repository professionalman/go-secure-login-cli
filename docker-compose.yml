services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    stdin_open: true
    tty: true
    volumes:
      - auth_data:/app/data

volumes:
  auth_data:
